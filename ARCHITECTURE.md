# 📘 Полное руководство: Архитектура и Функционал

## 1. Архитектура Проекта

Проект построен по модульному принципу. Это означает, что ядро бота отделено от конкретных команд (функций), что позволяет легко добавлять новые возможности без изменения основного кода.

### 📂 Структура файлов и их назначение

#### **Корневая директория**

* **`bot.py`**: **Главный файл (Точка входа).** Инициализирует бота, загружает конфигурацию, подключает базу данных, запускает веб-сервер и регистрирует все модули. Обрабатывает входящие обновления от Telegram.
* **`watchdog.py`**: **Сторожевой пес.** Отдельный процесс, который следит за здоровьем бота. Если бот зависнет или упадет, watchdog перезапустит его. Также он отвечает за отправку уведомлений о старте/остановке.
* **`deploy.sh`**: **Мастер установки.** Скрипт на Bash для автоматической установки, обновления и удаления бота. Настраивает Systemd или Docker окружение.
* **`.env`**: **Конфигурация.** Хранит чувствительные данные: токен бота, ID админа, пароли и настройки режимов.

#### **Директория `core/` (Ядро)**

Здесь лежит логика, обеспечивающая работу бота:

* **`auth.py`**: **Система авторизации.** Проверяет права доступа (Root, Admin, User), загружает список пользователей и управляет белым списком.
* **`config.py`**: **Менеджер настроек.** Загружает переменные из `.env`, настраивает логирование и пути к файлам.
* **`server.py`**: **Web-сервер (Aiohttp).** Отвечает за работу Web-панели управления (Dashboard) и API для приема данных от удаленных Нод.
* **`i18n.py`**: **Мультиязычность.** Отвечает за перевод интерфейса (RU/EN). Хранит все текстовые строки.
* **`keyboards.py`**: **Интерфейс.** Генерирует кнопки меню (Reply) и инлайн-кнопки (Inline) в зависимости от прав пользователя.
* **`utils.py`**: **Утилиты.** Вспомогательные функции: форматирование байт, проверка IP, работа с системными путями (важно для Docker).
* **`nodes_db.py`**: **База данных.** Работа с SQLite для хранения списка подключенных серверов (Нод).

#### **Директория `modules/` (Функционал)**

Каждый файл здесь — отдельная команда в меню бота:

* **`traffic.py`**: Мониторинг сетевого трафика в реальном времени.
* **`speedtest.py`**: Замер скорости интернета через `iperf3`.
* **`xray.py` / `vless.py`**: Управление VPN (обновление ядра Xray, генерация ссылок).
* **`notifications.py`**: Фоновые задачи проверки ресурсов (CPU/RAM) и отправка алертов.
* **`users.py`**: Управление пользователями бота (добавление/удаление).
* И другие (`uptime`, `top`, `reboot`, `sshlog`...).

---

## 2. Режимы работы и Роли

Бот может работать в двух режимах установки (`DEPLOY_MODE` в `.env`), от которых зависят доступные функции.

### 🛡️ Режимы установки (Install Modes)

1. **Root (Полный доступ):** Бот запускается с правами суперпользователя (или монтированием хоста в Docker).
* *Доступно:* Перезагрузка сервера, чтение системных логов (journalctl), управление сервисами, обновление системы (`apt upgrade`).


2. **Secure (Безопасный):** Бот работает от ограниченного пользователя.
* *Ограничения:* Нельзя перезагрузить физический сервер, нет доступа к системным логам SSH и Fail2Ban. Доступен только мониторинг и управление самим ботом.



### 👤 Роли пользователей

Внутри бота реализована иерархия доступа:

1. **Root (Супер-админ):**
* Имеет доступ ко всем функциям, включая опасные (перезагрузка, логи).
* Определяется переменной `ADMIN_ID` в `.env`.


2. **Admin (Администратор):**
* Может управлять пользователями, генерировать VLESS ссылки, запускать Speedtest.
* Назначается через меню "Пользователи".


3. **User (Пользователь):**
* Только просмотр статистики (Трафик, Аптайм, Статус).
* Не может менять настройки или управлять сервером.



---

## 3. Детальное описание функций (Модули)

### 📊 Категория "Мониторинг"

* **🛠 Сведения о сервере (`selftest.py`):** Показывает сводку: CPU, RAM, Диск, IP, Пинг, Версию ОС.
* **📡 Трафик сети (`traffic.py`):** Запускает живой мониторинг. Сообщение обновляется каждые X секунд, показывая текущую скорость (Мбит/с) и общий объем.
* **⏱ Аптайм (`uptime.py`):** Показывает, сколько времени сервер работает без перезагрузки.
* **🔥 Топ процессов (`top.py`):** Выводит список 10 процессов, наиболее нагружающих CPU.

### ⚙️ Категория "Управление"

* **👤 Пользователи (`users.py`):** Панель для добавления новых людей в бота по Telegram ID, смены их ролей или удаления.
* **🖥 Ноды (`nodes.py`):** Управление удаленными серверами (агентами). Позволяет переключаться между серверами и выполнять команды на них.
* **🔗 VLESS-ссылка (`vless.py`):** Генератор ключей доступа. Вы отправляете боту JSON-конфиг Xray, он генерирует готовую ссылку `vless://` и QR-код.
* **🩻 Обновление X-ray (`xray.py`):** Автоматически определяет установленную панель (Amnezia, Marzban) и обновляет бинарный файл Xray Core в контейнере.

### 🛡️ Категория "Безопасность" (Только Root)

* **📜 SSH-лог (`sshlog.py`):** Показывает последние попытки входа на сервер (успешные и неудачные) с флагами стран.
* **🔒 Fail2Ban Log (`fail2ban.py`):** Показывает последние IP-адреса, заблокированные системой защиты.
* **📜 Последние события (`logs.py`):** Вывод последних строк из системного журнала `journalctl` (ошибки, предупреждения).

### 🛠 Категория "Инструменты"

* **🚀 Скорость сети (`speedtest.py`):** Запускает тест скорости через `iperf3`. Автоматически ищет ближайший сервер, либо сервер в РФ/Европе в зависимости от геолокации.
* **⚡️ Оптимизация (`optimize.py`):** Запускает скрипт очистки кеша, удаления старых ядер и оптимизации TCP-стека (sysctl).

### 🔌 Управление питанием

* **♻️ Перезапуск бота (`restart.py`):** Перезапускает процесс бота (через Systemd или Docker).
* **🔄 Перезагрузка сервера (`reboot.py`):** Отправляет команду `reboot` хост-системе. Требует подтверждения.
* **🔄 Обновление VPS (`update.py`):** Предлагает выбор: обновить код бота (git pull) или пакеты системы (`apt upgrade`).

---

## 4. Web-интерфейс (WebUI)

Бот поднимает локальный веб-сайт (по умолчанию порт 8080), который служит графической панелью управления.

**Функции WebUI:**

1. **Дашборд:** Красивые графики потребления ресурсов в реальном времени.
2. **Настройки:**
* Изменение порогов уведомлений (например, слать алерт, если CPU > 80%).
* Настройка частоты обновления трафика.
* Управление видимостью кнопок в Telegram-меню.


3. **Логи:** Просмотр логов бота прямо в браузере.
4. **Сессии:** Просмотр и принудительное завершение активных сессий пользователей.

---

## 5. Система Нод (Мульти-серверность)

Бот умеет управлять не только тем сервером, где установлен, но и другими VPS.

1. **Сервер (Main Bot):** Основной бот, где вы нажимаете кнопки. Хранит базу данных всех нод.
2. **Агент (`node/node.py`):** Легковесный скрипт, который ставится на подчиненные серверы.
* Работает как веб-сервис.
* Принимает команды от Основного бота (например, "дай статистику CPU").
* Отправляет результаты обратно.
* Требует только Python и открытый порт.



**Процесс:** В меню "🖥 Ноды" вы создаете ноду -> Бот дает токен -> Вы запускаете скрипт установки агента на втором сервере с этим токеном.

---

## 6. Система уведомлений (Notifications)

Файл `modules/notifications.py` работает в фоне и проверяет:

1. **Ресурсы:** Если CPU/RAM/Disk превышают порог (настраивается в WebUI), админ получает уведомление.
2. **Даунтайм Нод:** Если удаленный агент перестал отвечать, бот пришлет алерт "Нода недоступна".
3. **SSH/Fail2Ban:** (Если включено) Уведомляет о каждом входе в систему или бане IP.
